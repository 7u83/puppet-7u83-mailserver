# AUTOMATICALLY GENERATED BY MAILMAN ON 2018-06-08 18:08:01 UTC
#
# This is your GNU Mailman 3 configuration file.  You can edit this file to
# configure Mailman to your needs, and Mailman will never overwrite it.
# Additional configuration information is available here:
#
# http://mailman.readthedocs.io/en/latest/src/mailman/config/docs/config.html
#
# For example, uncomment the following lines to run Mailman in developer mode.
#
# [devmode]
# enabled: yes
# recipient: your.address@your.domain

#[mailman]

#queue_dir: /var/spool/mailman
##log_dir: /var/log/mailman
##lock_dir: /var/lock/mailman
#etc_dir: <%= @mailman_etcdir %>
##ext_dir: /usr/local/etc/mailman.d

[mailman]
layout: custom
#log_dir: /var/log/mailman
#lock_dir: /var/lock/mailman
#ext_dir: /usr/local/etc/mailman.d


[paths.custom]
var_dir: <%= @mailman_vardir %>
bin_dir: <%= @mailman_bindir %>
queue_dir: <%= @mailman_spooldir %>
etc_dir: <%= @mailman_etcdir %>
ext_dir: <%= @mailman_extdir %>
pid_file: <%= @mailman_pidfile %>
log_dir: <%= @mailman_logdir %>


[mta]
incoming: mailman.mta.postfix.LMTP
outgoing: mailman.mta.deliver.deliver
lmtp_host: 192.168.1.125
lmtp_port: 8024
smtp_host: 192.168.1.125
smtp_port: 25
configuration: <%= @mailman_cfg %>
remove_dkim_headers: <%= @_remove_dkim %>

[webservice]
hostname: 192.168.1.125
port: 8001
use_https: no
admin_user: restadmin
admin_pass: restpass
api_version: 3.1


[database]
url: sqlite:////var/db/mailman/sqlite.db


[postfix]
postmap_command: <%= @mailman_postmap_command %>
transport_file_type: hash

